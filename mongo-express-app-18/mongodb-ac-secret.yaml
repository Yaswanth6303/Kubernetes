apiVersion: v1
kind: Secret
metadata:
  name: mongodb-ac-secret
type: Opaque #  Indicates a generic secret for storing arbitrary key-value data
data:
  # Credentials must be stored in Base64-encoded format, not in plain text.
  # Note: Base64 encoding is NOT encryption; it can be easily decoded.
  # To encode values, use the following commands:
  #   echo -n "root" | base64
  #   echo -n "pass" | base64
  # The encoded values are static unless the original values change.
  # In production environments, use secure secret management tools such as:
  # Doppler, HashiCorp Vault, or AWS Secrets Manager.
  mongo-root-username: cm9vdA==
  mongo-root-password: cGFzcw==
